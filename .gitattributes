# Логи слоёв только дописываются — параллельные ветки склеиваются, а не конфликтуют
# (методология Ledger Loop §4.6). Инварианты мержатся обычным образом: они маленькие,
# и конфликт в них заслуживает внимания.
context/**/log.md merge=union
